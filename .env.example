# Database
DATABASE_URL=postgresql://pensiuni:pensiuni@localhost:5433/pensiuni

# JWT
JWT_SECRET=change-me-to-a-random-secret-in-production
JWT_EXPIRATION=7d

# Cloudinary (leave as placeholder for local file storage)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# reCAPTCHA (Google test keys — always pass in dev)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="-"
RECAPTCHA_SECRET_KEY=""
# App URL
APP_URL=http://localhost:3000

# CORS origins for API (comma-separated)
CORS_ORIGIN=http://localhost:3000

# Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token

# Live Camera URLs (one per ski slope)
NEXT_PUBLIC_LIVECAM_OLIMPICA_URL=
NEXT_PUBLIC_LIVECAM_CASCADA_URL=
NEXT_PUBLIC_LIVECAM_PRISLOP_URL=

# API URL (used by Next.js rewrites to proxy /api/* to the backend)
API_URL=http://localhost:3001
